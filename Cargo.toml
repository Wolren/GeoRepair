[package]
name = "geo-repair"
description = "Fix invalid GIS geometries using optimal algorithms per type: CDT arrangement for polygons, noding for lines, structure method as fast path"
version = "0.2.0"
license = "Apache-2.0"
repository = "https://github.com/Wolren/GeoRepair"
keywords = ["gis", "geo", "geospatial", "validation", "repair"]
categories = ["science::geo"]
edition = "2024"
rust-version = "1.95"

[features]
default = ["arrange", "structure", "parallel", "simd", "validate"]
arrange = ["dep:spade", "geo/spade", "dep:rstar"]
structure = ["simd", "dep:rstar"]
validate = ["dep:rstar"]
parallel = ["dep:rayon", "geo/multithreading"]
simd = []
simd-portable = []

# Built-in WKB parser is always available — no extra deps needed
bench-geos = ["dep:geos"]
bench-criterion = []

python = ["dep:pyo3"]
ffi = []

proj = []
serde = ["geo/serde"]

[dependencies]
geo = { version = "0.33", default-features = false }
spade = { version = "2.15", optional = true }
rstar = { version = "0.12", optional = true }
log = { version = "0.4", default-features = false }
robust = "1.2"
thiserror = "2.0"
rustc-hash = "2.1"
smallvec = { version = "1.15", default-features = false, features = ["union"] }
serde_json = "1.0"

[target.'cfg(not(target_arch = "wasm32"))'.dependencies]
rayon = { version = "1.12", optional = true }
geos = { version = "11.1", optional = true, features = ["v3_8_0"] }
mimalloc = { version = "0.1", default-features = false, optional = true }
pyo3 = { version = "0.29", features = ["extension-module"], optional = true }

[dev-dependencies]
criterion = { version = "0.8", features = ["html_reports"] }
proptest = "1.11"
wkt = { version = "0.14", features = ["geo-types"] }

[lib]
name = "geo_repair"
crate-type = ["cdylib", "lib"]

[[bin]]
name = "geo-repair-cli"
path = "src/main.rs"

[[bench]]
name = "bench"
harness = false

[[bench]]
name = "real_world"
harness = false

[[bench]]
name = "criterion"
harness = false
required-features = ["bench-criterion"]

[profile.release]
lto = "fat"
codegen-units = 1
panic = "abort"

[profile.bench]
lto = "fat"
codegen-units = 1
panic = "abort"
