.history/
target/
benches/real_world/*
!benches/real_world/alaska.shp
!benches/real_world/alaska.dbf
!benches/real_world/alaska.shx
!benches/real_world/alaska.prj
!benches/real_world/alaska.bin
*.py
*.py[cod]
# The wheel's package init (re-exports the extension module) must be
# committed and shipped despite the *.py rule above.
!python/geo_repair/__init__.py
!scripts/readme_bench_table.py
patches/
# cargo-fuzz build artifacts (corpus/ is committed)
fuzz/target/
fuzz/artifacts/
fuzz/coverage/
/dist/

# Scratch run logs (errA.log, gate dumps, ...).
*.log
# proptest regression seeds are committed on purpose - see
# tests/fuzz.proptest-regressions and proptest-regressions/ - so do NOT
# ignore them here.

# Agent/instruction files must never be tracked
AGENTS.md
CLAUDE.md
.cursorrules
.hermes/
